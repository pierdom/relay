services:
  relay:
    image: ghcr.io/pierdom/relay:latest
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - relay_data:/data
    restart: unless-stopped

volumes:
  relay_data:
